<nav class="bg-slate-900 text-white shadow-sm">
  <div class="max-w-6xl mx-auto flex justify-between items-center px-6 py-3">

    <!-- ロゴ -->
    <%= link_to "TechLog", root_path, class: "text-xl font-bold tracking-wide" %>

    <!-- ナビ -->
    <div class="flex items-center gap-3 text-sm">

      <%= link_to "ログ一覧", posts_path,
        class: "px-3 py-1 rounded bg-slate-700" if current_page?(posts_path) %>

      <% if current_user %>

        <%= link_to "ログ投稿", new_post_path,
          class: "px-4 py-1.5 rounded bg-indigo-600 hover:bg-indigo-700 transition font-medium" %>

        <%= button_to "ログアウト", destroy_user_session_path,
          method: :delete,
          class: "px-3 py-1 rounded hover:bg-slate-700 transition" %>

      <% else %>

        <%= link_to "登録", new_user_registration_path,
          class: "px-3 py-1 rounded hover:bg-slate-700 transition" %>

        <%= link_to "ログイン", new_user_session_path,
          class: "px-4 py-1.5 rounded bg-indigo-600 hover:bg-indigo-700 transition font-medium" %>

      <% end %>

    </div>
  </div>
</nav>